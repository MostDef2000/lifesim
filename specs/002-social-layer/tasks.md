# Список задач: M2 — Социальный слой (Social Layer)

## Реализация схемы и сидирования
- [ ] T1: Создать таблицу `relationships` и `relationship_events` в `models.py`. Колонка `stress` УЖЕ существует в `character_health` (`models.py:135`) — не дублировать. Обновить `schema_meta` до 0.2.0. Готовность: Схема создаётся без ошибок через `create_all`, `character_health.stress` доступен в моделях.
- [ ] T2: Реализовать детерминированное назначение лидеров и членов организаций в `seed_world.py`. Готовность: В новом мире созданы организации, лидеры назначены согласно `leader_titles` или lowest-id.
- [ ] T3: Реализовать сидирование 2 пар конфликтов (`affection = -50`) при генерации мира через seeded RNG (детерминировано). Готовность: В БД после сидирования присутствуют ровно 2 пары с отрицательной симпатией; повторное сидирование с тем же seed даёт те же пары.

## Конфигурация и события
- [ ] T4: Добавить раздел `social:` в `default.yaml` и обновить `Settings` в `config.py`. Добавить параметры для `SOCIALIZE` в `actions` и `needs.recovery_rates`. Готовность: Конфиг загружается без ошибок, параметры доступны в коде.
- [ ] T5: Расширить `EventType` в `events.py`, добавив `SOCIAL_INTERACTION`, `RELATIONSHIP_CHANGED`, `CONFLICT` с соответствующими payload. Готовность: Enum обновлён, тесты на валидность payload проходят.

## Социальное действие и Utility AI
- [ ] T6: Реализовать валидатор для действия `SOCIALIZE` (проверка порога `social`, поиск цели по `affection`, порог отказа −60, обработка отказа). Готовность: Действие валидно только при наличии подходящего NPC (affection > −60); при отсутствии — возвращается False.
- [ ] T7: Реализовать логику автоматического перемещения (`MOVE`) в social hub (kitchen/settlement), если выбранная цель находится в другой локации. Готовность: NPC перемещается в хаб перед началом `SOCIALIZE`.
- [ ] T8: Интегрировать `SOCIALIZE` в `utility.py` и `registry.py`. Обновить формулу расчёта score. Готовность: Действие выбирается Utility AI при дефиците социальной потребности; позиция в реестре соблюдена.

## Жизненный цикл и динамика
- [ ] T9: Реализовать хук завершения `SOCIALIZE` в `lifecycle.py`: изменение `affection` по значению на момент начала (−5.0 + стресс +5 обоим при affection_at_start < −30, иначе +2.0), генерация соответствующих событий. Готовность: После завершения задачи состояние отношений и стресс обновлены, событие записано в лог.
- [ ] T10: Реализовать триггеры перехода между bands (stranger → acquaintance → friend / conflicted) с генерацией события `RELATIONSHIP_CHANGED`. Готовность: Событие генерируется ровно один раз при пересечении порога.
- [ ] T11: Реализовать восстановление потребности `social` во время выполнения `SOCIALIZE`. Готовность: Значение `social` растёт согласно `social_recovery_per_minute` в течение всей задачи.

## Инварианты, CLI и отчётность
- [ ] T12: Добавить новые инварианты в `invariants.py` (диапазоны отношений, целостность событий и членства в организациях, проверка стресса). Готовность: Все новые инварианты возвращают True при корректном состоянии мира.
- [ ] T13: Реализовать флаг `--social` в `cli.py` и добавить информацию об организациях в финальный JSON-отчёт. Готовность: Прогон с `--social` активирует социальный слой; в отчёте видны списки организаций.

## Верификация и приемка
- [ ] T14: Написать unit-тесты для обновления отношений (delta, clamp, refusal, conflict, band-crossing) (AE3). Готовность: Все тесты AE3 пройдены.
- [ ] T15: Провести интеграционный прогон 30д/20NPC с `--social` и проверить AE1 (события, инварианты, wall-time ≤ 60с). Готовность: AE1 полностью выполнен.
- [ ] T16: Провести интеграционный прогон 7д/20NPC (AE2) и проверку детерминизма (AE4). Готовность: AE2 и AE4 пройдены.
- [ ] T17: Провести регрессионный тест M1 (AE5) при `social.enabled = false`. Готовность: Вектор событий M1 идентичен, все тесты M1 AE1-AE8 зелёные.
- [ ] T18: Финальный гейт: запуск всех тестов проекта, проверка линтером и скриптом `scripts/check.sh`. Готовность: Все проверки пройдены.
