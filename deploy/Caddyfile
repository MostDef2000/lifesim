# ВЛ1: Рейнеке — Caddy reverse proxy (015-deploy-kit)
# Единственный публичный порт — 80/443 (Caddy). API и статика — через него.

# Замените example.com на ваш домен после делегирования DNS (см. deploy/README.md)
example.com {
	reverse_proxy 127.0.0.1:8000
	encode gzip
}
