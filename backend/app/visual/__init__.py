"""M6 (SPEC §105/66-70): visual generation layer.

Read-only over world state (П1-аналог); only visual_assets + storage_dir are written.
Off by default (П2-аналог): headless paths never import this package.
"""
